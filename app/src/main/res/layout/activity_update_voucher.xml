<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="16dp">

    <!-- Input untuk Nama Voucher -->
    <EditText
        android:id="@+id/etVoucherName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nama Voucher" />

    <!-- Input untuk Harga Jual -->
    <EditText
        android:id="@+id/etSellingPrice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Harga Jual"
        android:inputType="numberDecimal"
        android:layout_below="@id/etVoucherName"
        android:layout_marginBottom="16dp" />

    <!-- Input untuk Harga Modal -->
    <EditText
        android:id="@+id/etCostPrice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Harga Modal"
        android:inputType="numberDecimal"
        android:layout_below="@id/etSellingPrice"
        android:layout_marginBottom="16dp" />

    <!-- Tombol Update -->
    <Button
        android:id="@+id/btnUpdateVoucher"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Update Voucher"
        android:layout_below="@id/etCostPrice"
        android:layout_marginTop="16dp" />
</RelativeLayout>
